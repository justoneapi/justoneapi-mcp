# JustOneAPI MCP Server Configuration

# Required: Your JustOneAPI token
JUSTONEAPI_TOKEN=your_token_here

# Optional: API base URL (default: https://api.justoneapi.com)
# JUSTONEAPI_BASE_URL=https://api.justoneapi.com

# Optional: Request timeout in milliseconds (default: 20000)
# JUSTONEAPI_TIMEOUT_MS=20000

# Optional: Number of retries after first attempt (default: 1)
# JUSTONEAPI_RETRY=1

# Optional: Debug mode - logs to stderr (default: false)
# JUSTONEAPI_DEBUG=false

# Optional: Maximum items to return (default: 10)
# JUSTONEAPI_MAX_ITEMS=10

# Optional: Include raw response data (default: false)
# JUSTONEAPI_INCLUDE_RAW=false
